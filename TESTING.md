# How Danger Close Is Tested

This project's premise is *verify, don't trust* — so this page explains what verification actually exists, what it covers, and what it doesn't. It's written for the same skeptical reader the app is.

⚠ **`package_check` gained `G-2`, `KIND: handover` and section `J` on 2026-09-01.** `G-1` is structurally blind to NEW files — it `continue`s on any workspace file with no repo counterpart — so `G-2` covers hand-written files under `qa/`. Section `J` runs POST-SHIP against the pool and is the only check that can see what actually landed; pass the pool as a 4th argument and re-run after uploading. Controls: `qa/tools/controls_v5571.sh`. ⚠ **`P17` in `package_check_controls.sh` does not fire and did not before either** — verified against the unmodified gate. ⚠ **`v592` is the retired v5.9.2 leg, not an orphan** (`qa-baseline/README.md`); registries end in it deliberately, so a version roll must not key on the CURRENT tag being last. **Current build: v5.62** · source `src/DangerClose.jsx` md5 `827566da23ba3f37a3d7a66432afddfe` · **1,808 automated checks verify this build** (current leg 1,126 · parity 10 · feature suites run once 672); the prior v5.61 leg replays at 1,126, bringing a full run to **2,934 app checks, 0 failing**. ⚠ **Both legs are EQUAL at v5.62 and that is correct** — the release changes call-site WIRING, not the calculator, so `t10` §2E's five new behavioural checks hold on both legs ungated, while the argument-shape checks live in `t8` (run-once, source-reading). ⚠ **MC parity 10/10 is EXPECTED AND BLIND here**: `PORTFOLIO` carries no `stateCode`, so all three parity fixtures resolve to `stateCode: null` and never consult `STATE_RULES`. ⚠ **`t8` gained 2 checks (40 → 42) at NO version-ladder cost** because it is run-once; a new per-leg suite was written first, then deleted — `t8` already owned the class (`[EXTINCTION v5.56]`), already excluded the function definition, and already stripped comments before scanning, all three of which the new suite had to rediscover the hard way. Controls: `qa/controls_v562.sh` (six; C1a and C1b revert the defect's two halves SEPARATELY, because a partial fix would otherwise pass).

> ⚠ **This header had read v5.38 / 1275 checks for nine releases**, and the per-suite figures below
> had drifted with it. Corrected 2026-08-23 from parsed suite output, along with every other
> current-state figure on this page. The per-release paragraphs beneath are **historical records of
> the release they name** and are deliberately left alone — they were true when written. Anything
> describing the *present* is rolled; anything describing a *past release* is not. That distinction
> is the one this page keeps losing: a partly-rolled page is worse than an openly stale one, because
> it looks maintained.

**v5.38 taxes the ACA-premium sale's gain — the standing candidate since v5.34 — routes it into the IRMAA lookback, and was derived before it was built.** Since v5.34 the sale that pays a forfeited ACA subsidy realized gain into ACA MAGI but never into tax: the disclosed optimistic asymmetry, deferred at v5.36 (decision 5) precisely so it could ship alone under a parity witness. The build order ran in full: an independent reference ledger (own bracket walk, own §86 worksheet, own AMT/gross-up/contraction; law tables shared with the Verify tab's primary-source pins) first reproduced the shipped v5.37 engine **72/72 — every aggregate to the dollar, every year's wealth to the dollar, every subsidy to the cent** — then projected the fix, and the edited engine matched the projection **31/31** including the aggregate's second-order (the lifetime-tax delta is $3,802, *less* than the $4,028 sum of the gain taxes, because deeper pool depletion trims later dividends). The parity witness landed exactly as declared: `INTENDED_DIFFS["v537→v538"] = ["rothAca"]` — the ACA-guardrail fingerprint moved, and `roth`, `rothCurrentEstate`, and all three Monte Carlo fingerprints stayed **byte-identical**. `t22` was re-scoped at the boundary (group F's byte-identity claim expires by design, inverted behind a behavioral capability probe in the v5.34 idiom) and gained **group I**: the derivation pins ($314,708 / $1,150 / $8,908,031), the 0%-band identity (gain realized, tax $0, nothing else moves — measured to be the common case at modest conversions), the invariance pins, and the solver check. The gate caught two real things before ship: `lost` **can be negative** (a floor-crossing household's converting strategy out-earns its own baseline's subsidy; the shipped `lost ≥ 0` comment was false and the pool credit it protected is preserved), and the first candidate edit had dropped that credit. Two instrument findings the controls work surfaced, §B2-shape both: the re-pointed `controls.sh` had gone **entirely blind** — a stripped execute bit made every control silently test the clean build (the runner now aborts loudly on rebuild failure) — and group I's first gate was **circular** (a build with the tax reverted classified itself as "too old to test" and skipped the pins; re-gated on an adjacent source marker). Fifteen script controls fire (C1–C9, C12–C15), C15's unique signature being the IRMAA pin at $0 with ~$52 coupled knock-ons via the surcharge's own funding. The C10/C11 defect class got a **live-fire** verification nobody planned: a killed controls run leaked C8's payload into the build workspace, and the DOM diff's Taxes-identity check caught the dead Engine B call site (28/1) exactly as designed — the candidate was restored from its hash-pinned export and every artifact regenerated and re-verified. DOM: all four tax-bearing tabs at strict identity for this pair, including the Roth tab measured **identical** on the shipped example household once unseeded Monte-Carlo noise is blinded (its ACA gains sit in the 0% band). `smoke_built` 16/16 against the shipped `index.html`. Registered, not fixed: **neither** sale's gain reaches NIIT or state capital-gains — disclosed in METHODOLOGY, tracked for its own release.

**v5.37 fixes E-15 — ordinary money grows, and its growth is finally taxed — and the fix was derived before it was built.** Through v5.36, Engine D's ordinary-character balance (`taxOrd`) was depleted but never grown, so a $600,000 IRA under Other accounts recognised exactly $600,000 of lifetime ordinary income however much it compounded — `t20`'s exact-$600,000 pin was the defect's own fingerprint. The build order this project requires ran in full: an independent simulator (its own IRS Pub 590-B divisor table, its own ledger) first reproduced the shipped v5.36 engine **to the cent in every year across nine households**, then projected the one-line fix — and the edited engine matched the projection **to six decimals** ($724,266.004427 lifetime excess; trad−annuity 0.000000). The design is three interlocking decisions: one growth line (every moved figure has one cause), ordered caps (`taxOrd + taxGainPool ≤ taxable` by construction, the HSA share the untaxed remainder), and the sleeve's own growth rate (both sub-pools grow in lockstep, so the cap binds **zero** years — watched, not assumed: `t19` now carries the ledger in-suite, re-validates it against the published balances to the cent every run, and reports any binding year). The blast radius was measured by AST census before the edit: `taxOrd` is write-only into MAGI, so Engines A/B/C are byte-identical (parity 9/9 strict), the Taxes and IRMAA tabs *cannot* move, and the DOM diff flipped all three tax-bearing tabs to strict identity — the example household's lifetime MAGI rises $3,333 but never crosses a bracket edge. The release's divergence witness therefore lives at the engine level: `t19`'s exact MAGI pin ($3,162,820) and `t20`'s $724,266 pin, plus a new E-15 **extinction** (the excess must EXCEED the opening balance, so the omission cannot silently return). Thirteen negative controls fire, including the new **C13** (growth line reverted → t19(1)+t20(2)) and C10/C11 re-verified against the identity form. E-17 closed in the same release: `t20` and `t7` now declare the household they run (dob strings, measured value-identical to the object-dob runs), with `t20` E2's exacts documented as regime-bound to full pool exhaustion — the labelled 1962/1964 household was measured and rejected because both pins fail on the *unchanged* engine outside that regime. This session also stopped once mid-build: the ratified fixture dates collided with the ratified exact-0 gate, and the collision was reported with measurements rather than adapted around (the stop report and the maintainer's decision are in the session record).

**v5.40 corrects four disclosures that had drifted off their engines, and fixes two small-screen mechanics classes — and moves no figure.** Nothing here changes a computed result: three items rewrite prose about behaviour that is already right, and two change presentation and keyboard type. `t1` gains **8 extinction invariants** (+94 → **102**) pinning the three fixed classes, and **every one was proven to fail against a deliberately reverted build before being accepted** — reverting the S-1 sentence fails 4, removing a single `overflowX` wrapper fails 1, stripping five `inputMode` attributes fails 1. That step is not ceremony: this suite has recorded probes that died silently and read as green, and a green run cannot distinguish a passing check from a dead one. **App total 1,344 across 22 suites, 0 failed**, plus **16 built-artifact smoke checks** against `dist/index.html`.

*Per-suite at the **v5.40** ship (historical — the 22 suites that existed then):* `t1` 102 · `t2` 18 · `t3` 36 · `t4` 228 · `t5` 58 · `t6` 21 · `t7` 41 · `t8` 38 · `t9` 14 · `t10` 163 · `t11` 40 · `t12` 23 · `t13` 42 · `t14` 44 · `t15` 11 · `t16` 24 · `t17` 74 · `t18` 67 · `t19` 65 · `t20` 100 · `t21` 50 · `t22` 85.

*Per-suite at the **v5.47** ship (historical — the 29 suites that existed then, parsed 2026-08-23):* `t1` 155 · `t2` 27 · `t3` 36 · `t4` 228 · `t5` 58 · `t6` 21 · `t7` 41 · `t8` 38 · `t9` 14 · `t10` 163 · `t11` 40 · `t12` 23 · `t13` 42 · `t14` 44 · `t15` 11 · `t16` 24 · `t17` 74 · `t18` 67 · `t19` 65 · `t20` 100 · `t21` 50 · `t22` 85 · `t23` 25 · `t24` 38 · `t25` 35 · `t26` 25 · `t27` 18 · `t28` 34 · `t29` 43.

*Per-suite at **v5.59**, current leg, parsed from suite output 2026-09-02 — **32 suites**:* `t1` 185 · `t2` 35 · `t3` 36 · `t4` 252 · `t5` 58 · `t6` 21 · `t7` 41 · `t8` 40 · `t9` 14 · `t10` 224 · `t11` 40 · `t12` 23 · `t13` 42 · `t14` 44 · `t15` 11 · `t16` 24 · `t17` 74 · `t18` 67 · `t19` 65 · `t20` 100 · `t21` 50 · `t22` 85 · `t23` 25 · `t24` 38 · `t25` 45 · `t26` 25 · `t27` 18 · `t28` 34 · `t29` 54 · `t30` 12 · `t31` 31 · `t32` 12.
⚠ **`t10` moves 213 → 224 current and 213 → 217 frozen; `t31` 29 → 31 on BOTH legs.** The frozen v5.58 leg carries the four pre-fix `t10` pins and the two pre-fix `t31` keys; everything else on it is unchanged. MC parity 10/10 is EXPECTED and blind here — no fixture is domiciled in RI or WI.

*Per-suite at **v5.58**, current leg, parsed from suite output 2026-09-02 — **32 suites**:* `t1` 185 · `t2` 35 · `t3` 36 · `t4` 252 · `t5` 58 · `t6` 21 · `t7` 41 · `t8` 40 · `t9` 14 · `t10` 213 · `t11` 40 · `t12` 23 · `t13` 42 · `t14` 44 · `t15` 11 · `t16` 24 · `t17` 74 · `t18` 67 · `t19` 65 · `t20` 100 · `t21` 50 · `t22` 85 · `t23` 25 · `t24` 38 · `t25` 45 · `t26` 25 · `t27` 18 · `t28` 34 · `t29` 54 · `t30` 12 · `t31` 29 · `t32` 12.
⚠ **`t31` moved 28 → 29 on BOTH legs**, not only the current one: the v5.58 key is asserted on every leg, in its pre-fix form on v5.57. A key that moved only the current leg would mean the gating was missed.

*Per-suite at **v5.57**, current leg, parsed from suite output 2026-08-31 — **32 suites**:* `t1` 185 · `t2` 35 · `t3` 36 · `t4` 252 · `t5` 58 · `t6` 21 · `t7` 41 · `t8` 40 · `t9` 14 · `t10` 213 · `t11` 40 · `t12` 23 · `t13` 42 · `t14` 44 · `t15` 11 · `t16` 24 · `t17` 74 · `t18` 67 · `t19` 65 · `t20` 100 · `t21` 50 · `t22` 85 · `t23` 25 · `t24` 38 · `t25` 45 · `t26` 25 · `t27` 18 · `t28` 34 · `t29` 54 · `t30` 12 · `t31` 28 · `t32` 12.

*Per-suite at **v5.56**, current leg, parsed from suite output 2026-08-30 — **32 suites**:* `t1` 185 · `t2` 35 · `t3` 36 · `t4` 252 · `t5` 58 · `t6` 21 · `t7` 41 · `t8` 40 · `t9` 14 · `t10` 207 · `t11` 40 · `t12` 23 · `t13` 42 · `t14` 44 · `t15` 11 · `t16` 24 · `t17` 74 · `t18` 67 · `t19` 65 · `t20` 100 · `t21` 50 · `t22` 85 · `t23` 25 · `t24` 38 · `t25` 45 · `t26` 25 · `t27` 18 · `t28` 34 · `t29` 54 · `t30` 12 · `t31` 28 · `t32` 12.

*Per-suite at **v5.53**, current leg, parsed from suite output 2026-08-28 — **32 suites**:* `t1` 185 · `t2` 35 · `t3` 36 · `t4` 252 · `t5` 58 · `t6` 21 · `t7` 41 · `t8` 38 · `t9` 14 · `t10` 163 · `t11` 40 · `t12` 23 · `t13` 42 · `t14` 44 · `t15` 11 · `t16` 24 · `t17` 74 · `t18` 67 · `t19` 65 · `t20` 100 · `t21` 50 · `t22` 85 · `t23` 25 · `t24` 38 · `t25` 45 · `t26` 25 · `t27` 18 · `t28` 34 · `t29` 43 · `t30` 12 · `t31` 26 · `t32` 12.

> ⚠ **The tables above are the drift this page keeps having, and the v5.53 ship shows the shape of it.**
> That release rolled the header's *numbers* and left the same sentence's *claims* stale — it still
> named `v5.51→v5.52` as the DOM-diff pair and its `t31` key list stopped at v5.52 — while the
> per-suite table below it stayed at v5.47 / 29 suites, so `t30` and `t32` appeared on this page zero
> times each. Rolling a figure is not rolling a line. **Corrected 2026-08-28 from suite output run in
> that session; the per-suite figures above were generated from the tally file, not transcribed.**

**The cross-version DOM instrument was re-pointed on 2026-08-20 and is green again.**
`qa/domdiff_withdrawal.mjs` had been red since the v5.40 ship and three releases stale, still pointing at
v5.36 → v5.37. Its IRMAA region ran from `Tax YrAffectsMAGI` to `Not tax advice` — 1,972 characters, of
which the trailing ~1,070 were prose — so v5.40's corrected MAGI sentence fell inside a check whose name
says FIGURES. The Taxes region, by contrast, had always stopped on the last figure. The end anchor moves
to the first prose token: **913 characters, the year table and nothing else, byte-identical across the
pair**, with the divergence measured at offset 1,734 of 1,972 so every figure ahead of it already
matched. Three per-leg checks now witness the v5.40 sentence explicitly, gated so the frozen v5.39 leg
keeps replaying green. **Both controls fired**: killing Engine C's sole call site (AST-resolved, L9719)
fails the tightened identity check with headroom figures moving $138K → $139K; reverting the disclosure
fails exactly the two v5.40-side copy checks and leaves figures green. **`qa/controls.sh` remained the
v5.38 edition and could not run against the current pair** — the controls above were run by hand.
**Fixed at v5.42 and the stale file deleted 2026-08-21:** `qa/controls_v542.sh` was the re-pointed
successor, with all thirteen v5.38-era patch anchors verified to resolve exactly once against the
current source before the re-point, plus six new controls for the §86 work.

**Superseded again 2026-08-23 by `qa/controls_source.sh`, and the version is out of the filename**
— it never described the controls, only the one `SRC=` line, so it guaranteed the file LOOKED stale
the moment a tag rolled. It then sat unrun for five releases, which is most of how the next problem
went unseen. ⚠ **When it was finally run, four controls reported `*** NOT CAUGHT ***` and all four
verdicts were FALSE.** `t24_ss86_phasein` reads `dom_<tag>.cjs`; the harness rebuilt only
`app_<tag>.mjs`, so C16–C19 patched the source and left `t24` reading a clean build. Hand-verified
with the bundle rebuilt: C16 → 6 `t24` failures, C17 → 8, C18 → 8, C19 → 7, clean → 0. `t24` was
never blind. The harness now rebuilds every artifact a named suite consumes, and
`verify_artifacts()` refuses to print any verdict unless each is newer than the source it was built
from — because a harness that cannot prove the perturbation arrived has not earned `CAUGHT` **or**
`NOT CAUGHT`. **C0** was added at the same time: a comment-only edit that must NOT fire, since
every other control is expected to fire and a harness that rubber-stamped `CAUGHT` would have
passed all nineteen of its own checks.

**Extended 2026-08-23 — the gap is now largely closed.** Fourteen controls were added for
v5.43–v5.47, of which **thirteen fire**; the harness runs **32 controls, 0 uncaught, C0 correctly
silent**. What the work actually found was not missing controls but **missing fixtures**: three of
the fourteen reverted correct code that no household in the suite could reach. `t25` gained two
households to close two of them — §F, benefits small enough for the §86 half-cap to bind, and §G,
income low enough to enter the §86 **middle tier**, a band no fixture had ever entered. The
remaining one, the Roth tab's copy of the half-cap, is written but deliberately **not invoked**: it
would print `NOT CAUGHT` forever, and a permanent false finding trains the reader to ignore the
real one. Enabling it needs a low-benefits fixture in `t24`, which reimplements the ladder against
example-household constants, so a second household means re-deriving all of them.

⚠ **The residual gap.** These controls cover the v5.36 capital-gains
work and the v5.42 §86 phase-in. **Nothing covers v5.43–v5.47** — not the §86 half-cap, the
spouse-B claim gate, the HSA dividend base, or the Roth tab's RMD-exempt share. `t25`, `t26`, `t27`
and `t28` did not exist when these were written and have no source-level controls at all. A green
run says the v5.36 and v5.42 work is still covered; it says nothing about the last five releases.

**The published artifact rebuilds byte-identical from project knowledge alone** — verified 2026-08-20
for the first time, `dist/index.html` md5 `17867edb9af4c5e7e3542aeade594f24`, `qa/smoke_built.mjs`
**16/16, exit 0** including the disclaimer gate rendering and dismissing. The pool was missing
`src/index.html` until that date, so this had been asserted for eleven releases and never demonstrated.

**Since the v5.40 ship, `qa/`-only: `t1` 102 → 108, app total 1,344 → 1,350; and separately `qa/domdiff_withdrawal.mjs` 28/1 → 32/0 — cross-version tooling, counted in no app total.** The figures above are
what v5.40 shipped and are left as they are. Six **structural** checks were added to `t1` on 2026-08-20
against the same v5.40 source (md5 `6b7cebb1476ee66e57079b713b94ba75`, unchanged — no engine, constant,
prose or version-string edit, so this is not a release). They close the gap the v5.40 entry names as
outstanding: the four S-1 checks above are source-text and prove the IRMAA *sentence* is right without
being able to see the *engine*, which is how the same sentence was falsified twice. The new checks
resolve Engine C's `magi` expression by AST — by enclosing function, never by line number — and require
its term set to match a registered set exactly, order-insensitively, while requiring the two terms that
historically falsified the prose to be present in **both** the engine and the sentence. All six were
proven to fail against deliberately reverted builds before being accepted, and the full 22-suite baseline
was re-run on both legs plus parity: **1,350 passed, 0 failed**, parity 9/9, the v5.39 leg unchanged at
94. Coverage is deliberately narrower than "IRMAA MAGI": the checks are named for Engine C, because a
second, four-term MAGI in the Roth tab's render block also reaches the screen under the IRMAA label and
is **not** covered. No arithmetic has been run against that one; it is recorded as a thing to measure,
not as a finding.

**Two measurement corrections worth recording.** The scope estimated **28** money fields needing `inputMode` from a label heuristic; exact enumeration by what each input binds and parses found **47**, the 19 missed being fields whose labels carry no currency symbol. And the **version-bump tax was measured rather than recalled**: 35 sites name the current top version across five baseline suites, against the "~25 feature gates" figure carried in several session briefs, with **433** `VER ===` comparisons accumulated in total — `ARCHITECTUREIssues.md` E-7 recorded 202 across five suites at v5.29, so the ladder has more than doubled.

**`t22`'s prior leg is buildable after all.** It had been recorded as unrunnable because `app_v532.mjs` could not be reconstructed from knowledge. Built from the v5.38 source instead, `t22` runs its full prior-leg comparison: **74 passed / 1 failed → 85 passed / 0 failed.**

**Limitation, stated plainly.** The eight new invariants are **source-text** assertions. They prove the fix is present in the source; they do not prove it renders correctly at 380px, and no device or headless-layout verification was run this release. The structural assertion tying the IRMAA sentence to Engine C's `magi` expression remains separately scoped and unwritten.

**v5.36 realizes capital gains in the drawdown and taxes them, and the release was built twice — the second time because six of its own test files went missing.** Session 2 opened on a freshness check (OPERATIONS §A2) that contradicted its brief: six of the seven suite files said to carry session 1's edits were byte-identical to repo HEAD, the session-1 versions existing nowhere in project knowledge under any name — the fourth recorded pool-drift block, recovered only from the maintainer's archive and verified by reproducing the documented base exactly (1219, to the check). The engine work itself: Engine D tracks the gains-bearing brokerage sub-pool as its own balance and basis (hand-verified against an independent 25-year simulation to zero error in every year, lifetime gain $215,216 exact), and Engines B and C consume the per-year gain series through defaulted parameters fed by the app's call sites from the *selected scenario's* schedule. The new consumption surface is dollar-exact tested: `t18`'s pension-only fixture takes a $10,000 injected gain to MAGI +$10,000, ordinary federal tax +$0, LTCG tax +$1,500, NIIT +$380, total +$1,880 — every delta hand-computed before the engine confirmed it — and `t17` asserts exact MAGI and headroom pass-through into the IRMAA tier test. Parity **9/9 strict**; Engine A and the Monte Carlo/stress/Roth engines are byte-identical to v5.35, and the Withdrawal tab renders byte-identical too (the tracker is bookkeeping on top of an unchanged schedule — measured, not assumed).

**Twelve negative controls, and the instruments failed before the code did.** C1–C7 (the v5.35/session-1 set, rewritten with their patches embedded so they can no longer be lost with a session's /tmp) all fire; C8/C9 revert each engine's consumption and fire with clean orthogonality — C8 moves only Engine B's fingerprint and is caught by t18(6)+t19(1), C9 only Engine C's, caught by t17(5). Getting there surfaced three verdict-instrument failures of the exact shape §B2 warns about, all now codified in the adopted `qa/controls.sh`: a fingerprint probe that died silently and read as NO-OP (a dead probe now reports PROBE DIED, never no-op); a control patch that skipped `mk_testable.sh` so the DOM bundle was built from the stale splice and the control was a no-op at the bundle level; and — the one that redesigned a check — the first call-site witness asserted the Taxes/IRMAA tabs *differ* across the pair, and its own control (both call sites dead) **did not fire**, because the release's copy differs regardless. The witness is now anchored to figures-only regions past every piece of changed copy, and the control fails exactly the two witness checks. Those two checks matter because nothing else can see the call sites: every suite calls the engines directly, so a forgotten `gainByYr` at the call site leaves 1260 checks green while both tax tabs silently show no gains.

**One new defect class was found mid-build, pinned as a disclosed limitation, and then fixed in-release on the maintainer's decision.** Wiring real gains into Engine B made a dormant simplification live: its Social-Security provisional-income proxy omitted realized gains, which IRC §86 includes — an *optimism* on gain-bearing households with SS in the phase-in range. It was first pinned in `t18` as a dated `[DISCLOSED LIMITATION]` (a $100K gain leaves `ssTaxable` unchanged, asserted exactly) with the copy disclosing the omission; when the maintainer chose fix-now over own-release, the pin FLIPPED in place — the same fixture now asserts the fix exactly (the $100K gain drives `ssTaxable` to precisely the 85% statutory cap of IRC §86(a)(2), computed from the row's own `ssTotal`, and MAGI rises by exactly gain + ΔssTaxable) — the short-lived omission copy was corrected on both surfaces in the same session that wrote it, and control C12 reverts the term and fires t18(3). The copy pass inverted every disclosure lock it falsified, gated per leg (`t4`: current 209, prior 199 — the prior leg *gains* one assertion pinning the copy it is true to), including scope §9's label fix: the share is "% of brokerage money," not "% of taxable pool," because ordinary-taxed and HSA balances are structurally excluded from the gains-bearing pool. `t19` 57 (the two Engine-B/C extinction scans inverted in the same release the wiring landed, plus a C-specific binding) · `t17` 74 · `t18` 67 · DOM diff **26** (Withdrawal at strict identity again; Taxes and IRMAA at control-verified figure-region divergence).

**v5.35 takes the RMD out of the retirement account, and three defects were involved.** Engine D had been satisfying each year's required distribution by selling from the brokerage sleeve and handing the same cash back — balance-neutral, invisible for many releases, and identical on v5.33, so never a regression. Measured on a purpose-built household: **thirteen consecutive years with the buckets drawn ZERO while the RMD ran $85,740 → $224,021.** The second defect came from the same root — the same Other-accounts dollars tracked both as ordinary income and as the RMD-bearing subset, so **MAGI read $420,401 against a correct $307,906** in one household's first RMD year. The third was **created by this release and caught by an existing invariant before it shipped**: the new sleeve path booked income without spending the matching ordinary character, producing **$610,791 of lifetime ordinary income on a $600,000 account**. It was hand-computed to the cent — $10,807.67 of lifetime sleeve RMD less $16.28 left unspent = $10,791.39, against the engine's $10,791.40 — before anything was changed. Parity **9/9 strict**; Engines A, B and C are byte-identical to v5.34.

**Five negative controls, and three did not fire on the first pass. That was the finding, not a passing grade.** Reverting the sequencer moved `totalDrawn` **+34%** and ending wealth **−18%**; reverting the ordinary-income term moved lifetime MAGI by **+$452,599**; reverting the bucket draw term moved it **+25%** — each with the entire suite green. Each patch was measured to confirm it actually changed engine output before being called uncovered, because "the suite is blind" and "the patch was a no-op" look identical from a green run. The gap was closed by adding assertions until every control fired on **exactly one**, which required a **second Engine D household** rather than more assertions on the first: the original is all-bucket, so its sleeve RMD is zero and the character tracker is inert there. The new one — guaranteed income covering most expenses, so the pool survives to RMD age but a real spending draw still happens — also exercises the **shortfall path** and the bucket/sleeve split summing to the cent.

**Three assertions were found to be measuring nothing, and the total is smaller for it.** A companion pin that passed on *both* builds (the taxable pool grows across the RMD year either way, so it discriminated nothing); a disclosure check satisfied by copy shipped in v5.26; and a source scan that reported the very defect it described, because v5.35's own comment quotes the retired expression verbatim — AST resolution gives 0 hits against the scan's 1. Each was rewritten or removed with the reason recorded in the file, rather than counted.

**`domdiff_withdrawal.mjs` was run at strict identity first, and failed as it should** — 8/2, the divergence reading `Total portfolio draw$1.55M` against `Total withdrawn$1.21M`. It is now excise-by-anchor: three named regions removed, each asserted unique, bounded, and **verified to have actually changed**, so an excision cannot hide a no-op. With those removed the tab is byte-identical — this release touched Engine D's sourcing and two pieces of copy and nothing else on the tab. `t19` **22 → 32** · `t4` **191 → 198** current, **191** prior · DOM diff **10 → 20**.

**v5.34 ships Engine A only, and corrects four statements the app was making about it.** The conversion-tax funding model now tracks a real cost basis instead of applying one declared percentage forever, and the ACA cliff solver was corrected to match. Every other engine is byte-identical to v5.33 — parity **9/9 strict**. The drawdown half of the capital-gains work was **backed out mid-build and held for v5.35**: Engine D satisfies each year's RMD from the taxable sleeve and hands the same cash back as surplus, so attaching gain realization to that leg realized gain on money never sold — **83.7% of the example household's plan-life realized gain**, and **$24,657 in year one of a test household with no taxable account at all**. Conservative in direction, wrong in fact, and now a dated `[KNOWN DEFECT]` pin in `t19` with four extinction assertions guarding against the layer returning piecemeal. `t13` and `t17` returned to their v5.33 figures **with no test edited** — the proof condition for the backout.

**The copy corrections came with the coverage that was missing.** The app told users that paying conversion tax by withholding meant *"no sale, no gains tax"*. False on every build back to v5.9: withholding absorbs `min(conversion, bill)` of the year's **whole** tax and IRMAA charge, and any residual is sold from the brokerage and taxed. Measured at the shipped default share of 0, a $10K/yr conversion makes **19 funding sales realizing $111,359 of gain and $9,428 of capital-gains tax**. A sweep confirmed **no test asserted any of this copy**, which is why it survived nine releases (OPERATIONS §B2). `t4` now asserts the Field Manual and the live Roth tab, gated per leg, negative-controlled twice — restoring the manual's old copy fails 3 checks, restoring the Roth tab's fails 5, and each control fires only on the surface it corrupted. `t4` **176 → 191** current, **176 → 184** prior.

**Test addendum, 2026-08-14 (D-4) — `t14`'s source windows are bounded, not sized. No source change, no new build.** `t14` located each engine by a unique anchor and searched a **fixed character span** after it for that engine's Social Security survivor rule. A fixed span ages: as an engine grows around the rule, the rule drifts to the window's edge and eventually out, at which point the assertion fails and reads exactly like a regression in the app. Measured against shipped v5.33, Engine D had **588 characters of headroom** and Engine A **899** — and against the recovered capital-gains partial that v5.34 ports, Engine D's rule sits at +8,499, outside its 8,000 span, failing `t14` **32/1**. Windows now run from each anchor to the **start of the next top-level function**, so a window is "the rest of this engine" by construction. Both bounds are asserted unique file-wide, and a missing end marker fails loudly rather than falling back to a span. `t14` **33 → 44**, app total **1136**.

**A negative control did not fire, and that was the finding.** Weakening one of Engine D's two death guards left `t14` green at 41/0: the per-engine `death` check is a **presence** test, and Engine D has two guards in its window, so any surviving copy keeps the pattern matched. It read as "Engine D's death guard is correct" and meant "at least one death guard exists somewhere". Fixed by asserting the **absence of the weakened `>` form** — sound because **Engine D has no filing concept** (verified: no `yr > _deathYr1D` transition; filing status is Engine B's job), so a `>` there can only be a weakened guard. That is also why Engine D is legitimately absent from `filingEngines`, which had never been written down. Engines A/B/C carry the same presence-check weakness, materially narrower because `filingEngines` asserts a more specific pattern for each — **recorded, not fixed**.

**Four negative controls, all firing on exactly one check each**, and one decisive check: `t14` with bounded windows runs **44/0 against the partial**, the exact source on which the old span failed 32/1. The fix is verified against the code that will break the old one — which is why it ships ahead of v5.34 rather than with it.

**`t19`'s rebuild is NOT in this addendum.** It was planned for it; on reading the spec, essentially all 21 new checks assert capital-gains behaviour that does not exist at v5.33 (declared-share fidelity, the Option A pin, the recycling case, the per-year gain replay, and the *MAGI includes `capGain_y`* half of the rewritten B-2 pin). `t19` stays at **14** and rebuilds with the engine at v5.34. against that exact source, plus 16 more against the built `index.html` (md5 `c998f5ff760c6c5e04ab6173a68f6421`), plus 10 cross-version DOM-diff checks and 50 tooling checks counted separately.

**v5.33 adds a field, a reader, a schema default and a control — and moves no figure anywhere.** No engine reads the new `taxableGainPct` at this release, so the whole claim under test is *"this exists, persists, clamps, and changes nothing."* That is what the new checks assert and nothing more. `t1` gains 16 (the field's presence and default, the accessor's export, a nine-case clamp table, and AST checks that the accessor is defined once and **called by nobody**), `t5` gains 14 (persistence through Save & Apply, restore from a simulated pre-v5.33 backup that lacks the field, an unparseable value normalised to 0, a user-set non-zero value surviving a backup-bytes round trip, and an out-of-range value clamped by the accessor), and `t4` gains 17 (the panel renders, its label copy asserted verbatim, the control accepts a value, and Save & Apply writes it through to the module-level portfolio with out-of-range and negative entries clamped on save). Parity **9/9 strict** · `t1` 93 · `t4` 176 · `t5` 58 · app total **1125** · tooling **50**.

**Three witnesses carry "no figure moves", not one.** Parity is meaningful on this release precisely because the field is inert — anything that moved would show. It is backed by `qa/domdiff_withdrawal.mjs` at **10/10**, which renders the Withdrawal tab on both builds and finds the text identical apart from the version string, and by `t22` group F, which runs Engine A on an ACA-bridge household against v5.32 and requires the per-year subsidy map, `totAcaLoss` and `estate` to be byte-identical across all seven strategies.

**Three negative controls, all firing, each narrowly.** Removing the clamp's upper bound fails `t1` (1) and `t5` (1); removing the schema default fails `t5` (2); moving the shipped default off 0 fails `t1` (2), `t5` (2) and `t4` (1). A control that broke everything would prove no more than one that broke nothing. Each corrupted `v533.jsx` — the file `mk_testable.sh` actually builds from — because corrupting only the canonical `DangerClose.jsx` beside it leaves the bundle clean and the control silently does not fire.

**Two test defects were found and fixed during this build, both in the new checks rather than the app.** A static check counting call sites of `taxableGainShare()` by text reported three, of which two were comments; it was replaced with an AST node count rather than having its expectation relaxed. And a `t5` check that drove the real Export Backup button failed while the app was correct: `handleExport` serialises `buildPortfolio()`, the **form** state, so mutating the module-level portfolio without a re-render legitimately exports the old value. The bytes check moved to `t5` as a serialisation round trip and the control-driven check to `t4`, where the control can be typed into. A third, in `t4`, held a DOM node across Save & Apply — React replaces the input on re-render, so two clamp cases typed into a detached element and silently read the old value. All three are recorded at the point in the files where they matter.

**The prior leg replays at 520**, two above v5.32's 518: `t1` now asserts on every earlier leg that the field and accessor are **absent**. Each leg asserts what was true of it — gating, not a regression (OPERATIONS §B2).

**`t22`'s prior-build default rolls v5.31 → v5.32, with one assertion gated.** Its header has always said to roll the default forward each release, but doing so alone would break it: group F mixes byte-identity checks, true for any pair, with *"acaFloorYrs is NEW"*, a claim about the single v5.31 → v5.32 transition that is false once the prior build is v5.32. That one check is now gated on the prior tag, and `t22` holds at **64** on either pairing. This was forced by the knowledge rotation — v5.31 leaves the pool at v5.33, so the v5.31 bundle can no longer be built from knowledge alone.

**What the new checks do NOT cover, stated plainly.** There is no assertion anywhere that the field produces a correct capital-gains figure, because at v5.33 it produces no figure at all. The `t4` label assertions are the only coverage of the decision to ship the control ahead of the engines, and they are **disclosure locks**: they check that copy saying the model does not use the field is *present*, which will go green at v5.34 whether or not anyone remembers the copy became false. v5.34 must invert them, gated per leg, in the same release that falsifies them.

**Test addendum, 2026-08-14 — the ACA path is now inside the parity guardrail. No source change, no new build.** `src/DangerClose.jsx` is untouched at `7e7be3f8…` and `index.html` at `ef42fb0b…`; this changed only `t2` and the documents. `t2`'s original Roth fingerprint household is built with `acaPremium: 0`, and `acaHeads` returns 0 whenever the premium is not positive — so `bridgeInWindow` was false and **no ACA code executed inside the cross-version parity guardrail at all, from v5.7 through v5.32.** A second, fully explicit, premium-positive household now fingerprints under the key `rothAca`; **parity moves 8/8 → 9/9** and `t2` gains three coverage assertions on each leg. App total **1078**.

**The gap was demonstrated, not argued.** Deleting the 100%-of-FPL floor constant outright — one line, reproducing the pre-v5.32 defect exactly — was run against both versions of the guardrail on the same pair of builds: **`t2` as shipped passed it 8/8 with zero failures**; `t2` with the addendum fails on `rothAca` and on nothing else. Because the pair is still v5.31 → v5.32, this also brings v5.32's own ACA behaviour retroactively inside the guardrail rather than starting coverage at the next release.

**What it does not cover.** The enhanced regime. `ACA_REGIME` is a module-level `let` whose only assignment is inside a React handler, so a module-level harness cannot switch it without a source change — and this addendum deliberately makes none. `t22` groups A/B/D still assert the enhanced branch directly against the statute. `ARCHITECTUREIssues.md` **E-15 is downgraded High → Low, not closed**, and the remaining half is scheduled to fold into the A3 release.

**v5.32 fixes the ACA floor in the enhanced regime, and flags — but does not remove — the sub-floor discontinuity.** The 100%-of-FPL eligibility floor (§36B(c)(1)(A)) was tested inside `acaApplicablePct`'s current-law branch only; the enhanced branch returned before ever reaching it, so under that toggle any ratio below 150% FPL yielded an applicable percentage of 0 — the entire benchmark premium paid as subsidy, measured all the way down to a ratio of 0.000. The floor is now tested before the regime branch and binds in both. Nothing in the suite had exercised any ratio below 1.0, in either regime, through all 1010 checks at v5.31; the new **`t22` (64 checks)** closes that. Four Verify rows are added, so the in-app check count moves **62 → 66**. Parity **8/8 strict** — with a large caveat, below. App total **1074** · tooling **50**.

**Read what parity proves on this release, because it is less than it looks like.** `t2`'s Roth fingerprint household is built with `acaPremium: 0`, and `acaHeads` returns 0 whenever the premium is not positive — so `bridgeInWindow` is false, `baselineSubByYr` is null, and **no ACA code executes inside the parity guardrail at all, in either regime.** Strict parity here is evidence that the non-ACA engines are untouched and evidence of nothing about this release's subject. The claim that v5.32 moves no figure under current law is carried instead by **`t22` group F**, which runs Engine A on an ACA-bridge household against the prior build and requires the entire per-year subsidy map, `totAcaLoss` and `estate` to be byte-identical across all seven strategies. This is the third instance of the blind-spot class in this project and the widest; it is recorded in `ARCHITECTUREIssues.md` as **premium-zero**, not merely current-regime.

**What v5.32 does NOT fix, stated plainly.** Sub-floor years still show **$0**, and one dollar of MAGI across the line still moves the modelled subsidy by nearly a whole benchmark premium. The release makes that artifact visible and excludable — the engine records which bridge years fall below the floor and at what depth, and the strategy table names them and excludes them from its improvement claim — but a household below 100% FPL is still being shown a placeholder rather than an answer. The pre-build measurement across six reconstructed households found **6 of 24 modelled bridge years sub-floor**, two of them below 51% of FPL, which is why the flag carries the depth of each crossing rather than only its existence.

**v5.31 moves the OBBBA senior-bonus constants into the verified set and moves no figure.** The four statutory figures behind the deduction were inline literals inside `computeTaxPlan`, so the Verify tab rendered green on constants it had never checked. They now live in a named `OBBBA_CONSTS` block with five Verify rows (four values plus a dated 2028 sunset row). The same release corrects a false footnote on the Taxes tab, which said the deduction was not modelled while the tab's own header and line item showed that it was. `t1` gains 11 checks (constants asserted against statute, extinction of the four literals, and the D-3 independence check) and `t4` gains 9, all gated per leg. Parity **8/8 strict**. `t1` 77 · `t4` 159 · app total **1010** · tooling **50**.

**v5.30 corrects a false OBBBA disclosure and moves no figure.** The Field Manual and METHODOLOGY had said the OBBBA senior bonus deduction was not modelled; Engine B has modelled it all along, so both statements were false and stated the wrong direction of error. `t18` gains three hand-computed OBBBA cases (including the 2029 sunset) and `t4` six checks on the corrected copy, gated per leg. Parity **8/8 strict**. `t4` 147 · `t18` 50 · app total **985** · tooling **50**.

**2D's gap is closed, not re-described.** The control that removed the never-behind branch from the shipped source used to change nothing, because the suite reimplemented the logic. It now fails two checks. `t10` gains 8 assertions calling the shipped function, four of them unit cases on synthetic series that isolate the branch from the engine.

**The prior leg replays at 990** — 20 fewer, because the v5.31 leg asserts the new constants block, the extinction of the four inline literals and the corrected Taxes-tab copy, while every earlier leg asserts the state true for its own build: no block, literals still inline, old footnote still present. Each leg asserts what was true of it (OPERATIONS §B2); that is gating, not a regression.

**A prior-leg break was caught during the build**: the Montana assertion initially ran on every leg and failed against v5.28, which legitimately carries the old note. Same mistake as v5.27, named by the §B2 rule added afterwards. Gated per build — history asserts the pin, v5.29 asserts the fix.

**Audit sub-phase 2E discharged 2026-08-12, and PHASE 2 IS COMPLETE (harness only, no version bump).** `t10` gains a **2E** block of 20 checks; total 134 → **154**, app total 947 → **967**. `stateTaxAnnual` is one function with five behavioural branches, and 2E verifies one jurisdiction of each — no-tax (FL, also the maintainer's own state), flat (AZ), `retExempt` (MS), `excl65` (AL, both filing statuses), and partial-SS (MT, which carries both an SS factor and an exclusion) — plus the fallback path and five clamps where a sign error would produce a negative tax. Every figure was computed from the rule table by hand first, then compared; all match to the cent. **Five negative controls, all firing.**

**All 51 jurisdictions were scanned for behaviour contradicting their own `note` string** — the specific defect the sub-phase scope names. Seven candidates surfaced and six were false positives (AGI thresholds and explicitly unmodelled provisions, not exclusions). One is real and pinned: **Montana taxes half of federally-taxable Social Security and its note never says so**, where all seven other partial-SS states do — asserted as a control so the pin cannot pass by accident. The modelling is correct; the disclosure is incomplete.

**Phase 2 — Section C, "numerical validation to the dollar" — is now verified across all five sub-phases** (2A 76 · 2B 78 · 2C across t11–t15 · 2D 19 · 2E 20). Two residual gaps are stated rather than closed: 2D's crossover selection expression is unexported and therefore reimplemented by the suite, and Montana's note. Neither affects a computed figure. See `AUDIT_2E_STATE_AND_PHASE2_ROLLUP.md`.

**Audit sub-phase 2D discharged 2026-08-12 (harness only, no version bump — the source is unchanged at v5.28).** `t10` gains a **2D** block of 19 checks; total 115 → **134**, app total 928 → **947**. The Roth break-even half of 2D had been recorded across three revisions of its findings document as "a premise reading, not a verification". It now verifies the identity that produces the crossover deficit — **year-one wealth delta = −(incremental federal tax) × (1 + GROWTH)** — hand-checked at $2,196 × 1.045 = $2,294, agreeing to the dollar of rounding. All three outcomes are covered on one fixture so that conversion size is the only variable: a real deficit that recovers ($30K/yr → 2048), a household **never behind** ($10K/yr, zero incremental tax — the branch 2D could not confirm was reachable), and one that **never breaks even** ($60K/yr → null). The discounting-equivalence claim asserted in the source comment is verified by measurement rather than reasoning: an isolated delta compounds at exactly 1.045 across eleven years.

**One negative control did not fire, and that is a recorded gap rather than a pass.** The selection expression `beYr = beWasBehind ? firstRecover : firstAhead` lives in an anonymous closure three levels inside `DangerCloseMain` and is exported nowhere, so the suite necessarily reimplements it. The wealth series, the tax identity and the equivalence are genuine engine output; the shipped one-line selection is not reached. Closing it needs the expression extracted to a callable function — a source change, out of scope for an audit. Recorded in `AUDIT_2D_BREAKEVEN_v5_28.md` and at the point in `t10` where it matters.

**v5.28 changes text and nothing else.** A `DOCS_HTML` audit against v5.27 found no false statement — v5.27 removed the last of those — but three places where the Field Manual was silent or stale: the Withdrawal Strategy entry still described the pre-v5.26 priority order, §13 Limitations named none of the five v5.26 simplifications that `METHODOLOGY.md` already carried, and the what's-new section announced v5.7 as "this build" twenty releases on. All three corrected; `t4` gains 13 assertions, one per claim plus two extinction checks.

**Parity 8/8 strict**, every v5.27 figure identical except t1's four STATIC version strings. **The prior leg replays at 915 with `t4` at 128** — the new assertions are gated to v5.28+, which is the §B2 rule added after the v5.27 harness patch working as intended on its first use.

**A bounded-edit near-miss worth knowing about.** The first attempt at the third edit used an end marker that was neither asserted unique nor sanity-checked and silently deleted 25,000 characters — a quarter of the Field Manual. The parse check caught it, because removing that much left the string literal unterminated; had the deleted span happened to be balanced, it would have parsed. Bounded edits inside `DOCS_HTML` now assert marker uniqueness, assert the end follows the start, and print the span length and net file delta.

## Re-running the parity negative control

The claim that `t2`'s ninth key actually guards the ACA path is checkable in about a minute, and
should be re-checked whenever that household or the ACA code changes:

```
sed 's/cliffMult: 4.0, floorMult: 1.0,/cliffMult: 4.0, floorMult: 0,/' v532.jsx > v532x.jsx
./qa/mk_testable.sh v532x
cd qa && node t2_engines.mjs v532 && node t2_engines.mjs v532x
node t2_engines.mjs compare v532 v532x
```

**Expected: 8 passed, 1 failed — failing on `rothAca` and nothing else.** Failing on nothing means
the household no longer reaches the ACA code and the key is decorative. Failing on other keys means
the corruption leaked somewhere it should not have. Delete `v532x.jsx`, `qa/app_v532x.*` and
`/tmp/t2_v532x_fingerprint.json` afterwards, and regenerate the real fingerprints — the corrupted
run overwrites nothing, but the stray files will confuse the next session's hash check.

## Running the suite — three copies and an ordering trap, neither previously written down

Both cost a session's time on 2026-08-13 and neither is a defect.

**After `mk_testable.sh`, four files must be copied into place** or ten suites die at module load in a way that reads like a broken build:

```
cp qa/app_<current>.mjs qa/app_testable.mjs     # t7, t8, t19, t22
cp qa/dom_<current>.cjs qa/dom_bundle.cjs       # t9, t11-t14, t16
cp <current>.jsx DangerClose.jsx                # t8, t19 — at the RUN-FOLDER ROOT
cp METHODOLOGY.md .                             # t31 — at the RUN-FOLDER ROOT
```

The qa-baseline README records the first two as a setup line and the third only implicitly, as a precondition on the working folder. All four are listed here because a missing copy produces an import error, not an assertion failure, and an import error is the easiest thing in this project to misread as a regression. Note also that t8, t11–t14, t16, t19 and t22 take **no version argument** — passing one is harmless, omitting a copy is not.

⚠ **The fourth line was added 2026-08-28 and the omission was live for four releases.** `METHODOLOGY.md`'s presence at the run-folder root has been required since `t31` landed at v5.49, and it was recorded in **§L5 of this file** and in the qa-baseline README — but not *here*, in the block a session actually copies from. A session following this list alone therefore lost both `t31` legs to a `FATAL: METHODOLOGY.md not found`, twice, after already losing five suites to the first three lines. **A precondition recorded somewhere other than the checklist people execute is not recorded.** `t31` fails loudly and legibly, which is the only reason this cost minutes rather than a session.

**Parity must run AFTER the prior leg, not before it.** `t2_engines.mjs compare` reads `/tmp/t2_<prior>_fingerprint.json`, which only exists once the prior leg's own `t2` has written it. Running parity first gives `ENOENT` and a stack trace with no explanation. `run_all.sh` and `runsuite.sh` both order it correctly; a session driving suites by hand may not. (`VERIFY.sh` also did, but it was **RETIRED at v5.49** — eleven releases stale and referenced by no operational document. Its header preserves this caveat.)

**`validation/` is a separate suite and its checks are NOT in any count above.** It is the older
public-constants suite, kept deliberately for a different purpose (`OPERATIONS.md` §B), and it is
**not part of the release gate**. Verified 2026-08-25 against v5.49: `check_constants.mjs` runs
**48 checks, 0 failing**, each carrying a statutory citation, and it covers constants this suite does
not touch — `SGL_LTCG`, `SGL_NIIT`, the partial-SS state count and the QCD cap among them. Its
behavioral half had been **unrunnable entirely** since `package.json` gained `"type": "module"`, which made node reject the CommonJS runner outright; renaming it `run.cjs` was the whole fix. ⚠ **Five of its six tests now run** (`smoke_entry` 26/26 tabs, 0 runtime errors) — **`deep_test` CRASHES**, driving a Roth solve-for RUN button that no longer exists, so the only modelling-shaped assertions in that directory are currently unchecked. See `validation/README.md`.

## The part you can check yourself, right now

Open the app → **Verify tab**. Every statutory constant the math depends on — federal brackets, deductions, LTCG thresholds and NIIT, IRMAA tiers, RMD divisors, the QCD cap, 402(g) contribution limits, the SS wage base, state-module facts, the longevity engine, and the ACA premium-subsidy tables — is re-checked **live in your browser on every visit** against its cited primary source (IRS Rev. Proc. 2025-32 and 2025-25, Rev. Proc. 2021-36, CMS, SSA, HHS/ASPE, IRS Pub. 590-B). Currently **54 checks**, each showing ✓/✗ and its citation. A tampered, corrupted, or stale copy fails loudly. This is the user-facing guarantee, and it requires trusting nobody: the checks and their expected values are visible in the page source.

Honest scope: the Verify tab proves the *constants* are right — not every formula that uses them. That's what the development suite below is for.

## The development test suite

**The suite is published in this repo** — `qa/qa-baseline/` (the t1–t6 regression baseline and its harness) and `qa/` (the t7–t9 feature suites). It runs in full before any release, and can be re-run from a clean clone; `qa/qa-baseline/README.md` has the setup steps. This was not always true: the original suite lived only in build-session sandboxes and was lost, which is why v5.10 rebuilt it as repo files.

The suite compares **two builds by role** — the current release and the immediately-prior one — and re-baselines every release. For v5.48 that pair is v5.47 → v5.48.

### Current build (v5.52) — 742 checks in the t1–t6 baseline plus t10

*(Rolled again at v5.52 from parsed suite output — the header had been stale at **v5.48** for three releases, and t1 (155), t2 (27) and t4 (228) were stale with it, directly beneath the note below that records the same failure. t1 now 180, t2 35, t4 249. The note stands as written and the header re-broke its rule anyway; a figure nothing checks drifts.)*

*(Corrected at v5.42: this header read **"Current build (v5.28) — 436 checks"**, fourteen releases stale, and the per-suite counts in the table below were stale with it — t1 read 64 against an actual 121, t4 read 141 against 228. Every figure in this section is now parsed from suite output. Roll them together or not at all; a partly-rolled table is worse than an openly stale one, because it looks maintained.)*

| Suite | Checks | What it covers |
|---|---|---|
| t1 units & statics | 180 | Pure functions and source invariants against hand-derived values: tax math, SS taxability, RMD divisors, state rules, parsing, the ACA machinery, plus a headless run of the in-app Verify checks and assertions that the version string is bumped at all four in-app sites — as of v5.10.2 all four are asserted exactly (footer, DATA LOAD header, Field Manual callsign, Field Manual footer) |
| t2 engines | 35 | Monte Carlo statistical properties, extended-MC longevity/LTC machinery, stress scenarios, Roth engine invariants, each reduced to a seeded fingerprint |
| t3 Roth | 36 | The conversion engine against hand-computed dollar figures, using explicit hand-built inputs rather than the demo household: bracket fills, IRMAA caps, funding-model gross-ups, widow transitions, and the ACA bridge cases |
| t4 DOM | 249 | The real component mounted in jsdom: all 26 tabs clicked and asserted for signature content, no NaN/undefined anywhere, Simple Mode round-trip, unsaved-edits guard. Signature strings were grounded against a captured DOM, not guessed | **v5.24 adds 17 extinction checks** on the corrected Priority 1 copy and the Field Manual: eleven read the Withdrawal tab's rendered text, six read the Field Manual's `srcdoc` attribute — the manual reaches the DOM only through an iframe, so reading `textContent` there would have passed vacuously on both builds. Negative-controlled: restoring the false copy fails 15 of the 17, the two survivors being the guards that prove the iframe assertions can fail at all. **v5.25 adds 16 more**, covering the Other-accounts tax-type selector, its recorded-but-unused disclosure, and the constraint that Traditional and Roth rows offer no Joint owner. These are scoped to the Other accounts card: a page-wide `select` query also catches the Holdings table's five owner selectors and reported 14 rows for a 9-row list. Negative-controlled — altering the disclosure fails 1, restoring Joint on retirement rows fails 3 |
| t5 persistence | 58 | The full storage lifecycle against the same `window.storage` contract the standalone build installs: fresh boot → landing, save → persisted, remount → reopens, backup export/import round-trip, and that a backup never contains the API key. As of v5.10.2, Clear All Data is tested against a fully-seeded store and asserted by **looping the `STORAGE_KEYS` map itself** — every key, including any added in a future release, must be absent after the wipe, and the seeded third-party contact PII must be gone from all surviving storage. **v5.25 adds 5**: a schema change reaches the storage contract, and a migration that works forward but corrupts an old backup would not show up in parity. The exported blob is asserted to carry a valid `taxType` on every Other account, to still total $111,000 Traditional, to hold no jointly-owned retirement account, and not to persist the transient migration flag |
| t6 single-filer | 21 | Storage seeded with a single-filer plan *before* mount, so the app boots into the branch the couple-centric demo never exercises. **v5.25 adds 3** for the degenerate ownership case: a single filer's Traditional or Roth Other account has exactly one possible owner, so it is rendered as fixed text rather than a one-item dropdown pretending to be a choice |

### Cross-version and feature suites

> ⚠ **ERRATUM, open as of v5.52.** This heading read *"1059 checks listed below"*, the correction note beneath it says **740**, and the table's own Checks column sums to **450** across **11** rows — three numbers for one table, none agreeing, and the table is missing t23–t31 entirely. The measured figures for v5.52 are in the header at the top of this file and in the CHANGELOG, both parsed from suite output. This heading's number is removed rather than replaced: rebuilding the table is a real piece of work and inventing a fourth total would be the same mistake a fourth time.

*(Re-derived at v5.42 from parsed suite output: 740 = the 731 feature checks plus the 9 parity checks. Several rows had drifted badly — `t17` read 63 against an actual 74, `t18` 50 against 67, `t19` 22 against 65, `t20` 94 against 100, `t22` 75 against 85, and parity 8 against 9. Note this table includes `t2 parity`, which is also counted in the release total below, so this number describes the table and must not be added to the baseline section to reach a release total. The release total is derived separately, below.)*

| Suite | Checks | What it covers |
|---|---|---|
| t2 parity (v5.47 → v5.48) | 10 | Under common seeded random numbers with identical inputs, the Monte Carlo, extended MC, stress, and Roth engines produce **byte-identical** output across the two builds. This is the mechanical form of any "engines unchanged" claim |
| t7 accrual | 41 | The v5.10 contribution-accrual feature against hand-computed figures ($96,000 / $24,000 / $72,000 for the couple case), migration parity, and round-trip persistence. Authored *before* the engine edits |
| t8 invariants | 38 | Extinction invariants (fixed defect classes asserted not to return), Verify-tab constants, and engine behavior. At v5.11 the two source invariants that asserted the old pooled Traditional seed in the Taxes and IRMAA engines were updated to assert the per-person split explicitly — a strictly stronger check. **At v5.22, +6 for the taxable-residual consolidation; re-pointed at v5.26** when the consumers moved up one level: `taxableInitFromPositions` is now reached through exactly ONE door, `taxableInitAll`, which adds the after-tax Other accounts, and the seven consumers call that. The suite asserts the residual expression appears exactly once (inside the helper), that `taxableInitAll` appears 8 times (1 definition + 7 call sites), and — as an extinction check — that the Roth funding gate no longer sums EVERY Other account as spendable. This is a stronger invariant than the v5.22 one it replaces |
| t9 DOM smoke | 14 | End-to-end render smoke over the feature surface |
| t12 Engine D survivor | 23 | The v5.12 extinction invariant for finding C-2C-4: the Withdrawal tab must drop to one Social Security check **and** apply the survivor spending factor at the first death. Asserts **both** movements in **both** direction configurations, checks the spending drop is specifically the shared 0.75 factor rather than an arbitrary reduction, and confirms nothing changes while both spouses are alive. **Read what this suite asserts, not what it is called:** it discriminates on *survivor* behaviour, and — established by negative control at v5.23 — **not** on Engine D's figures generally. A $50,320 swing in `totalDrawn` passes it. `t19` is the suite that discriminates on those figures. The two fixes are pinned together deliberately: correcting the Social Security half alone would leave the tab optimistic, so a future change cannot restore one without the other. **Precision: ±$500** |
| t11 survivor RMD | 40 | The v5.11 extinction invariant for audit finding C-2C-3: RMDs must follow each spouse's **own** age and start age, and a survivor's distributions must follow the **survivor**, not the deceased. Four household shapes: **A older** and **A younger** (the defect's sign flips with the age order, so a one-directional test would have passed against it in the other sign); **B dies first / A survives**, which exercises the other half of the spousal rollover; and a **SECURE 2.0 start-age straddle** (1959 vs 1960 births split 73 vs 75) covering the *pre-death* half of the defect. Includes IRS Uniform Lifetime divisor and start-age grounding read from the engine's own helpers. **Precision: ±$500** |
| t13 Engine C IRMAA survivor | 42 | The v5.13 extinction invariant for finding C-2C-5. The IRMAA planner had **three** survivor omissions that did not share a direction, so all three are asserted together: the survivor keeps only the larger Social Security check, is scored against the **Single** thresholds from the year *after* the death (IRS Pub. 501), and is charged for **one** person rather than two. Run in **both** direction configurations. **The example household cannot demonstrate this defect at all** — its survivor income never approaches even the Single threshold — so every case uses a purpose-built household landing inside the $109,000-wide band where the defect lives. A third case isolates the person count on its own by holding the tier constant across the fix. **Precision: ±$500 MAGI / ±$50 surcharge** |
| t14 cross-engine survivor | 44 | The class-level invariant (audit decision D-5). Three findings across two releases were the same failure — one engine not modelling a death the others did — so this asserts that **all four** engines carry the survivor larger-check rule bound to their own first-death year, and that those exposing a figure move it in the **same year**. The structural half is the only cover for the Roth strategy engine, whose per-year Social Security the harness cannot reach; that limit is stated in the file rather than dressed up as arithmetic. **Precision: ±$500** |
| t15 Engine A death-year filing | 11 | The v5.14 extinction invariant for finding C-2C-6, and the **first survivor suite that is dollar-exact**: the Roth strategy engine is module-level, so the harness drives it directly instead of reading figures rounded to the nearest $1,000. Asserts the death year is filed MFJ and the year after Single, in both direction configurations, against hand-computed bracket arithmetic. Includes the corner where the defect ran **non-conservative** — very high MAGI, where filing Single halved the IRMAA person count faster than it narrowed the thresholds. **Precision: dollar-exact** |
| t10 tax cases | 163 | **Adopted into the routine run at v5.14** (held since v5.10.2 for "the next release with an independent reason to exist"). **Four blocks: 2A federal core 76 · 2B+2C IRMAA 39 · 2D Roth break-even 27 · 2E state-tax module 21 = 163.** ⚠ This row read *"76 federal-core assertions plus 39 IRMAA"* — 115, describing only the first two blocks against a stated total of 163 — until 2026-08-23. Separately, the suite's own printed labels overlapped until the same date: its "2B" line was evaluated against a running counter at print time, so it reported 2B+2C+2D+2E while the 2D and 2E lines reported those blocks again, and adding the four labels gave **211** against a true 163. The v5.47 session made exactly that error and drafted a false finding from it. Read `t10 total`, not the sum of its parts. Its two dated `[KNOWN DEFECT]` indexation pins are **flipped** to the CMS-correct answers here — written from primary source before the fix existed. Its 30 tier-border cases were re-derived against premium-year indexing; ten correctly failed on the first run after the fix. **Precision: dollar-exact** |
| t16 Roth ladder filing status + age-65 deduction | 24 | The v5.15 extinction invariant for finding C-2B-3. The Roth tab's ladder ran its own tax arithmetic with the **married** deduction, brackets, SS thresholds and IRMAA cliff hardcoded, and no single-filer branch. Asserts the single filer's deduction, bracket and tax against an **independent** hand computation from IRS Rev. Proc. 2025-32 — not the app's own tables — plus that the couple's ladder does **not** move, that no private threshold arithmetic survives in the block, and that survivor years switch a year after the first death. **Precision: ±$500** |
| **t17 Engine C dollar-exact** | **74** | The payoff for the v5.17 hoist: drives the module-level `computeIrmaaPlan` directly, with no DOM in the path, and asserts to the cent. Tier borders at **±$1** on all five boundaries in both filing statuses (20 checks — the IRMAA cliff, testable for the first time), premium-year indexation, the statutory top-tier freeze, the 2-year lookback, per-person counts, the survivor filing switch, and QCD exclusion. Thresholds typed from the CMS figures published 2025-11-14, **not** read from the app. The surcharge *amounts* are deliberately NOT asserted CMS-exact — the constants are a disclosed rounding (≤$5, labelled approximate in the source, closed by the Phase 2B audit); instead the suite asserts that $5 **bound** from the primary source, so the rounding cannot widen unnoticed. **Negative-controlled:** reintroducing F-2B-1 fails 23 checks (measured when the suite held 63; it now holds 74 — the control was not re-run, and the ratio is a record of that measurement, not a current one). **Precision: exact** |
| **t18 Engine B dollar-exact + A/B agreement** | **67** | Drives module-level `computeTaxPlan` directly, no DOM. Federal ordinary tax against a hand bracket walk (3 incomes x 2 filing statuses), the age-65 extra counted per spouse incl. the pre-65 boundary, and SS taxability at the IRC §86 tiers. References typed from IRS Rev. Proc. 2025-32, **not** read from the app. **Case 10 is the headline:** Engines A and B are parallel implementations — A calls none of `fedOrdinaryTax` / `ltcgTax` / `marginalBracket` — and this asserts they agree, with A's inputs derived from B's own row so misconfiguration cannot fake a divergence. They agree on that path — but all six cases are pure ordinary income (SS, LTCG, NIIT, AMT, FICA, state all zero), so AMT in particular is NOT compared. **Negative-controlled at 24/47** (measured when the suite held 47; it now holds 67 — a record of that measurement, not a current ratio). NOT yet covered: LTCG stacking, NIIT, AMT, FICA, state tax, survivor transition. **Precision: exact** |
| **t19 Engine D (module-level)** | **65** | **New at v5.23, and Engine D's first discriminating coverage of any kind.** Drives the newly-hoisted `computeWithdrawalPlan` directly. Five structural checks (reachability, the 17-key return contract, determinism under a seeded RNG, and parameter purity — that the three inputs are genuinely parameters and not component-scope reads) plus eight fixture and pinned checks. Carries three dated `[KNOWN DEFECT]` pins: the taxable pot equals the whole of `otherAccounts` to the dollar including a named traditional IRA and an HSA; `magi` omits draws from the taxable pot; and money in a named IRA never reaches the balance RMDs are computed on. **Negative-controlled twice** — simulating the next release's MAGI fix drops it to 12/13, and simulating the release after that drops it to 11/13. **Not yet dollar-exact against hand-computed law** — the hoist is what makes that possible, and it is the next release's work. **Precision: exact where asserted** |
| **t20 Other-accounts tax type** | **100** | **New at v5.25.** The schema, the migration, and the extinction assertion that no engine reads the new field — which is what makes this release's "every figure identical" claim mean something. Covers: the shipped example household declaring types explicitly rather than relying on inference; migration from a v5.24-shaped backup; **the required equality** that inference over that household yields exactly $111,000 Traditional / $21,000 Taxable / $15,000 HSA, reproducing what v5.24 published to users; all four inference rules at their boundaries, including that "Roth IRA" resolves Roth and not Traditional, and that "Sepulveda" and "Simpleton" do **not** false-positive into `sep`/`simple`; the constraint that no Traditional or Roth account survives migration jointly owned, with the back-fill *ordering* asserted because the owner promotion depends on the type; a JSON round trip; the notice payload, including that it reports what it could **not** classify; and the defaults produced by every entry path. **The extinction check is a permutation test, not a name check:** the same household is run twice with every `taxType` flipped to its opposite and all five engines must return byte-identical output. **Negative-controlled five ways** — see the release notes. Two of its assertions were caught passing *vacuously* on v5.24 and now assert their precondition first. **Precision: exact** |
| **t22 ACA 100% FPL floor** | **85** | **New at v5.32.** Seven groups. **A** pins the floor as an `[EXTINCTION]` set at ratios 1.001 / 1.000 / 0.999 / 0.900 / 0.505 / 0.233 / 0.000 in **both** regimes, and asserts the 400% cliff still bites under current law and still does not under enhanced — getting the floor right by accidentally applying the cliff everywhere would be a different defect. **B** regime symmetry: the two regimes must no longer differ by a whole benchmark premium below the floor, and must still differ above it, so the fix does not neuter the toggle. **C** the boundary hand-computed to the cent from HHS/ASPE and Rev. Proc. 2025-25 **typed independently, not read from the app** — at exactly 100% FPL, one dollar above, one dollar below — plus the four `acaBelowFloor` cases that distinguish a statutory $0 above the cliff from an unmodelled blank below the floor. **D** the drift case: FPL grows 2%/yr and a flat MAGI does not, so the same household falls through the floor with nothing about it changed. **E** Engine A end-to-end on a bridge household that crosses **twice and at two depths** (~87% and ~18% of FPL), asserting exactly the crossing years are flagged, no others, that no unflagged bridge year pays $0, and the §6 solver guard. **F** the byte-identity group described above. **G** five negative controls, each corrupting `floorMult` and requiring the corresponding assertion to fail — including the one specific to the enhanced-regime fix — plus a check that the constant was restored. **Precision: exact** |
| **t23 Roth ladder RMD term** | **25** | **New at v5.41.** The extinction invariant for the omitted required-minimum-distribution term on the Roth conversion tab. Asserts that the RMD reaches all three expressions it belongs in — the IRMAA MAGI figure, the §86 provisional-income base, and gross taxable income — and that the tab's two disagreeing Traditional-balance projections were unified onto one. **Split by precision, and it says so:** the ladder table renders MAGI rounded to the nearest $1,000, so those assertions are **±$500**; the RMD cards render full dollars, so the distribution itself, its Pub. 590-B prior-31-December basis, and the recursion unification are pinned **to the dollar** ($44,991, with the grown-balance mistake excluded by name at $47,016). Runs at the $70,000 conversion default, where v5.42 correctly moves nothing — which is what makes it the proof that release did not overreach. **Precision: ±$500 ladder / dollar-exact cards** |
| **t24 §86 upper-tier phase-in** | **38** | **New at v5.42.** The extinction invariant for the §86 cliff: through v5.41 the Roth ladder assigned the full 85% of benefits the instant provisional income crossed the adjusted base amount, skipping §86(a)(2)'s phase-in and overstating taxable Social Security by up to 5.3×. **The defect is worth $0 at the $70,000 conversion default**, so assertions at the default prove nothing about it — which is precisely how it survived. This suite therefore **drives the conversion slider**, through the native `HTMLInputElement` value setter because the control is a React controlled input, to **$15,000, $20,000, $30,000, $50,000 and $70,000**, and checks all twelve ladder years at each. Expectations are not hardcoded: they are computed from `statute86` in `qa/tools/hand_86.mjs`, transcribed from the statutory text at law.cornell.edu, **not from any app expression** — the same oracle that produced the release's expected figures, deliberately, because at v5.41 a second independently-written derivation had silently drifted and the brief's table shipped wrong twice over. The **v5.41 leg asserts the old cliff figures** against the same recursion, so its greenness also proves the ladder transcription is faithful rather than coincidentally right. Carries a dated `[KNOWN DEFECT]` pin for the **middle** tier, which caps §86(a)(1) at 85% of benefits where the statute caps it at ½ — bounded by sweep at $2,468 joint / $1,850 single, needing benefits under $12,000, and $0 on the example household. **Precision: ±$500 (§M ceiling; the effect is $4,200–$38,030)** | |
| **t25 Engine C §86** | **45** | **New at v5.43**, extended at v5.47. The dollar-exact companion to `t24`: the IRMAA engine applied a flat 85% of benefits where §86 phases in, and this pins the three years the correction moves and the twelve it must not. **Both sets are named**, so neither leg can drift onto the other's numbers, and the shape is the assertion — twelve years moving item 5's flat $300 and three moving $555 = $300 × 1.85 is the signature of a change to the dividend base seen through §86, and NOT of a change to the §86 term itself. **v5.47 adds §E**, the extinction invariant for holding the HSA out of the dividend base. It lives here rather than in `t17`/`t18` because both of those build households with `P.otherAccounts = []`, so the fix is worth $0 on their fixtures **by construction** and an invariant added there would pass vacuously forever. **Precision: dollar-exact** |
| **t26 no-conversion RMD span** | **25** | **New at v5.44**, re-pointed at v5.47. The counterfactual RMD was grown from the as-of year while seeded at the ladder start, compounding a three-year gap twice. Its oracle is derived live from the timeline, not hardcoded — which meant it carried the annuity defect `v5.47` removed, so v5.47 re-seeded it from `rmdInit*`: **upstream** of the app's own route rather than parallel to it, since `retireStartBalances` builds `rmdInit` from the positions and then *derives* `annShare` from it. Mirroring the app's expression would have been tautological. **§E is item 6's extinction invariant**: both card halves must move (one basis, the v5.41 failure), spouse A unmoved as the negative control, plus an assertion that the control *can* fire. **Precision: dollar-exact cards** |
| **t27 §86 half-benefits cap** | **18** | **New at v5.45.** §86(a)(1) caps the includible amount at ½ of benefits; two mirror-image sites had dropped that cap. Both are asserted together, because fixing one alone leaves the tab and the engine disagreeing. **Precision: dollar-exact** |
| **t28 spouse-B claim gate** | **34** | **New at v5.46.** The Roth ladder credited spouse B's Social Security in every projected year regardless of B's claim date. Carries **two** fixtures deliberately: the first (B delays to 70, January claim) cannot distinguish a pro-rata gate from a whole-year one, so the modelling decision would have shipped unverified — the second moves B's claim to July and asserts all three candidate behaviours **by name**, so a regression toward either wrong branch fails by name rather than by an uninterpretable number. **Precision: dollar-exact** |
| **t29 boundary census** | **43** | **New at v5.46.** Covers the boundary census required at scope time (OPERATIONS §K1) — the rows recording which behaviours change at a threshold, and which households sit on one. Asserts the census is complete and internally consistent rather than re-deriving the engines. **Precision: structural** |**The built artifact is verified too, not just the source.** A green source suite says nothing about the single-file `index.html` that actually gets published — it is produced by a separate build step, and a build made from a wrong bootstrap entry can render perfectly while its persistence layer is dead. `qa/smoke_built.mjs` therefore exercises the *built* file before every release: it boots the artifact, renders and dismisses the first-open disclaimer gate, mounts React, loads the example household, reaches a data tab, and round-trips the `window.storage` persistence shim (set/get/list/delete, the prefixed localStorage write-through, and the throw-on-missing-key contract). **16 checks.** This was added at v5.11 after a build was produced from a reconstructed entry file and passed every check that existed at the time while being unable to save a plan. |
| **t30 legible defaults** | **12** | **New at v5.48.** The declared type floor — 12px body, 11px label — and the width guard on the five fixed-pixel grids that have to carry it. Carries a pre-v5.48 branch so the frozen leg asserts its own state (793 declarations below 11px, smallest 7px, columns at 50px), meaning a revert fails loudly. Locates the 146,679-char `DOCS_HTML` line **by length rather than a pinned index**, and asserts the exclusion removed exactly one line, so the floor count cannot be measured over the wrong text. ⚠ **Precision: DECLARED values only.** jsdom performs no layout, so nothing here sees a rendered box, and a green run says nothing about whether the app looks right |**Two honesty notes on t11.** First, *precision*: the Taxes and IRMAA engines are computed inside the app's component body, so the harness — which exports module-level bindings — cannot reach their row arrays. Their only output path is the rendered DOM, which prints every figure rounded to the nearest $1,000. t11's assertions are therefore accurate to **±$500, not to the dollar**. That is adequate here only because the effect being measured (~$4,050/yr per $1M of Traditional balance) is roughly eight times the measurement band. Making these engines dollar-exact testable requires a new harness capability and is scoped separately. Second, *teeth*: a test that cannot fail proves nothing, so t11 is run as a **negative control against the pre-fix v5.10.2 build**, where it correctly fails **six** discriminating assertions — both post-death directions and the pre-death start-age straddle. Three of its checks pass on the defective build too, and the suite says so rather than implying otherwise: the two cross-checks are supporting context, and the *survivor = A* case cannot catch the original defect at all, because the old pooled model was coincidentally correct when person A was the survivor. That case earns its place as forward-looking cover for a code branch no other case executes, not as evidence the defect is caught.

**1584 checks verify this build** = 906 (current leg, incl. t10 and t23–t29) + 10 (parity) + 668 (t7–t9, t11–t20, t22), every figure computed from parsed suite output rather than restated by hand. Reported as **1584 app + 90 tooling** — `t21` verifies the parser toolkit and the DOM diff compares two builds' rendered output; neither verifies this build's arithmetic. Re-proving the frozen prior leg (890) brings the full run to **2474 app checks, 0 failing**. *(This line has now gone stale four times: 928 through v5.30, **1078 through v5.34**, **1189 through v5.41**, and **1385 through v5.46** — disagreeing with the header above it every time. Rolling it at v5.47 required rolling nine other figures on this page, and adding seven suites the page had never listed. Roll them together or not at all.)* Frozen prior legs are re-proven at every run as history, and are expected to show since-fixed defects in their pre-fix state — that's correct, not a regression.

**Tooling checks are counted separately, and deliberately not added to the number above.** `qa/tools/`
is the sanctioned answer to "how many sites?" and "where is this used?" — its output is quoted in scope
documents — but until 2026-08-11 the toolkit itself had never been tested. `qa/t21_tools.mjs` now runs
all four parsers against a fixture with hand-counted known answers: **50 checks, negative-controlled six
ways.** *(This sentence read 49 while the sentence below it read 50, for eleven releases.)* They verify the *tooling*, not the app, so folding them into the app's total would inflate a
number that is supposed to mean "checks that verify this build." Reported as **1584 app + 90 tooling** — the tooling figure is `t21`'s 50 plus the DOM diff's 40. *(This sentence read "872 app + 49 tooling" from v5.22 until v5.35, "1189 app + 50 tooling" until v5.42, and "1385 app + 82 tooling" until v5.47 — the same drift, in a third place.)*

**Negative controls for v5.42.** Six perturbations of the §86 phase-in were run against the fixed
build. Reducing the phase-in slope from 85% to 50% fails **10** of `t24`'s checks; dropping the
`½(adjusted base − base)` term fails **8**; removing the overall 85%-of-benefits cap fails **8**;
shifting the adjusted base amount by $10,000 fails **7**; hardcoding the base amount back to the
married literal — reverting the v5.15 filing-status selection — fails **1** in `t1`.

**The sixth is the one worth reading.** Changing `para1`'s cap from ½ of benefits to 85% — the exact
defect that still stands in the middle tier — did **not** fire `t24`. Per the rule below, that was
investigated rather than adjusted away, and it is not a blind spot: `para1` enters the phase-in as
`min(para1, ½(adjusted base − base))` = `min(para1, $6,000)`, so the ½-benefits cap can only change
the result when half the benefits are under $6,000 — the same "benefits under $12,000" condition as
the middle-tier defect itself. The example household's benefits are $15,600 then $55,200, so the
perturbation cannot move any figure the tab renders for it. It **is** caught, structurally, by `t1`'s
STRUCT S-3 pin. The reasoning is now asserted in `t24` as D-7 rather than left as a note, so a future
change that makes it wrong fails a check instead of surviving in prose.

**One stale assertion was found and gated in the same release.** The DOM diff's check that the *prior*
leg carries the pre-v5.40 IRMAA MAGI sentence had been left pair-specific with a note to re-point it,
and the note was never acted on — so it had been false for every prior leg from v5.40 onward and went
red at the v5.41 → v5.42 pair with no code change to explain it. It is now gated per leg, which is the
same §B2 rule that governs disclosure assertions: each leg asserts the copy that was true for its own
build.

**Negative controls for v5.14.** Every new or changed assertion was run against the pre-fix v5.13 build and the result recorded per case. `t10` fails **23** — the two flipped indexation pins plus the border cases whose thresholds moved. `t15` fails **8** of 11; the three that pass are named in the file as not discriminating rather than counted as wins. `t14`'s new filing-timing assertion fails on **exactly one** engine, the one that was wrong. `t13` needed its fixture re-tuned — see its header for why the engine was right and the fixture was not.

**Negative controls for v5.25.** Every new assertion was broken on purpose. `t20` run against the frozen v5.24 build fails **42 of 59**; patching Engine D to actually read `taxType` makes the extinction assertion fire; breaking one inference rule drops the Traditional total to $104,000, short by exactly the $7,000 annuity; removing the owner promotion fails the schema invariant, both promotions, the ordering check and the notice; swapping the back-fill order fails the ordering assertion; altering the disclosure text fails one `t4` check; restoring Joint on retirement rows fails three. **Scrutinising what still *passed* on v5.24 was the part that mattered** — it caught two of the new assertions passing vacuously, because a condition about a field that does not exist is never true. Both now assert their precondition first. That is the negative-control rule catching its own tests failing to obey it.

**Negative controls for v5.15.** `t16` was run against pre-fix v5.14, where it fails **9** of 21 — the single-filer deduction, bracket and tax assertions, all four source assertions, and the survivor deduction switch. Two of its assertions pass pre-fix and are named inline as not discriminating rather than counted as wins. One case was rewritten mid-build: its first version computed an expected IRMAA threshold from constants and compared it to itself, a tautology that passed on both builds. The file records that, because a test which never touches the app cannot fail for the right reason.

**Parity returned to its strict form at v5.15.** The `INTENDED_DIFFS` entry added at v5.14 is keyed to the v5.13→v5.14 pair only, so v5.15 needed none — which is the mechanical proof that the Roth *tab* fix never reached the shared Roth *engine*.

**A note on what parity means from v5.14.** The MC-parity guardrail exists to catch *unintended* engine drift, so a release that changes an engine on purpose has to say which one and still prove the others are untouched. v5.14 corrects the Roth strategy engine, so its two parity legs are asserted to **differ**; the Monte Carlo, extended MC and stress legs must stay byte-identical. Asserting the intended change is stronger than skipping it: a later edit that silently reverted the fix would fail here.

### How known defects are tracked

When a bug is found but not yet fixed, it gets a dated `[KNOWN DEFECT]` test asserting *today's wrong behavior*, so the defect stays visible and "green" describes reality rather than hiding it. Fixing it means changing the code and flipping the pin to a positive assertion — the fix is then self-verifying.

Three pins were opened at the v5.10 baseline rebuild and **flipped at v5.10.1** (the ACA cliff solver's own funding sale, Clear All Data not wiping the API key, and a phantom Spouse B on the SS tab for single filers). The current leg now asserts the fixed behavior; the frozen prior legs keep the dated pins as pre-fix history. A fourth defect class was pinned and fixed in the same release at **v5.10.2** (audit Finding B-2): `clearStorage()` deleted only 10 of the 13 storage keys, leaving the checklist's third-party contacts, the Simple-Mode flag, and the SS-cut scenario in browser storage after "delete everything." The current leg asserts the full wipe via the loop invariant; frozen legs carry the dated pre-fix pins.

## Hand-verified ACA figures — check these with a calculator

These test cases were derived by hand from the primary sources and asserted to the dollar. Every row below was **re-verified against the v5.10.2 engine** for this release (byte-identical engine parity with v5.10.1 at 8/8; the v5.10.2 diff touches no engine). Sources: IRS Rev. Proc. 2025-25 (applicable percentages), HHS/ASPE poverty guidelines (2025: $15,650 + $5,500/person; 2026: $15,960 + $5,680/person), Rev. Proc. 2021-36 (ARPA table for the "enhanced" scenario).

| Case (single filer, 2026 coverage, $1,500/mo benchmark = $18,000/yr) | Value |
|---|---|
| FPL, 1 person (2025 guidelines govern 2026 coverage) | $15,650 |
| 400% cliff | $62,600 |
| Applicable % at MAGI $30,000 (ratio 1.9169, band 150–200%: 4.19→6.60) | 6.1996% |
| → Subsidy at MAGI $30,000 | $16,140.12 |
| Applicable % at the cliff (ratio 4.0 exactly — inclusive) | 9.96% |
| → Subsidy at MAGI $62,600 | $11,765.04 |
| → Subsidy at MAGI $62,601 | $0 |
| Enhanced scenario, MAGI $50,000 (ratio 3.195, ARPA band 6→8.5%) | 6.487% → subsidy $14,756.39 |

The v5.10.1 cliff-solver fix was hand-verified the same way, computed independently from the 2026 HHS guidelines and the Rev. Proc. applicable-percentage table before being compared to engine output.

Every row is reproducible from the cited documents with arithmetic. If you find a discrepancy, that's a bug report — please open an issue.

## A green suite is not a coverage claim — disclosed at v5.23

**A suite passing tells you nothing about what it would have caught.** This page has previously
reported check counts in a way that invited the opposite reading, and one release made that concrete.

While preparing v5.23, a deliberate +10% inflation perturbation was introduced inside Engine D — the
Withdrawal tab's projection — as a negative control. It moved `totalDrawn` by **$50,320**. The entire
757-check suite stayed green. `t4`, 90 checks that walk the rendered DOM including the Withdrawal tab,
passed. `t12`, 23 checks *named* `t12_engineD_survivor`, passed. **Engine D had never had
discriminating coverage of its figures, and nothing in the suite's output said so.**

Three things follow, and they are now standing practice rather than a one-off note:

- **A suite's name is not a coverage claim.** `t12` discriminates on survivor behaviour. It does not
  discriminate on Engine D's arithmetic. Both statements were always true; only the second was
  unstated.
- **Coverage is demonstrated per claim, by negative control** — break the thing on purpose and watch
  the suite fail. If nothing fails, the coverage was imaginary. `t19` and `t17` carry their negative
  controls in this page's table for that reason.
- **A green suite could not have proven the v5.23 hoist safe.** What proved it was
  `qa/domdiff_withdrawal.mjs`, rendering the tab on both builds and diffing the text.

The counts on this page are honest about how many assertions run and pass. They have never been, and
are not now, a measure of how much of the app is meaningfully watched.

## What a version bump actually costs (measured 2026-09-01)

**A version bump is priced in this project as "four in-app sites"** — footer, DATA LOAD header, Field
Manual callsign, Field Manual footer. **That is the SOURCE cost only.** The suite cost is an order of
magnitude larger, and until this entry existed **no document recorded it anywhere**.

Measured against v5.57 with `qa/tools/vercensus.cjs`:

| | from v5.57 | from v5.58 (measured 2026-09-02) |
|---|---|---|
| suite files that must register the new tag | **15** | **15** |
| ladder entries (mechanical append) | **16** | **17** |
| **gated expressions (each a judgement)** | **62** | **63** |
| total judgement points | **78** | **80** |

The cost grows by one ladder and one gate per release that lands a `t31` key (its `since:` entry and
its `since ===` branch), which is why it is re-derived every time rather than copied forward.

`t4_dom.mjs` alone carries **21** gated expressions and is where to start, because they are the ones
most likely to cover changed copy.

**The registries are fail-closed**, which is them working. An unregistered tag halts the suite:

```
✗ FATAL: version tag "v558" is not registered in this suite.
  Add it to the version ladders in this file BEFORE running.
```

⚠ **The 62 are not a script.** Each asks whether the new build makes that assertion false. Extending
them blindly applies a new expectation to frozen legs that legitimately still contain the old copy —
the v5.28 defect, applied once per gate.

**Why this entry exists, and it is the point.** The v5.54 stop-report measured this cost, called that
scope *"wrong by roughly a factor of sixty,"* and closed with the finding that **no document records
it**. That gap was never filled — so on 2026-09-01 `SCOPE_VA_NOTE_CORRECTION.md` §3C priced a bump at
*"four in-app sites"* and made the identical error, with the stop-report naming it sitting in the same
repo. **Re-derive the number with the tool rather than quoting the table above**; a figure written into
prose is exactly what goes stale (§A0).

## Known gate defects — filed here because they had no home (added 2026-09-01)

⚠ **`package_check` has no KIND for "built and verified, release documents not yet written"**
(found 2026-09-02). It models three: `app-release` requires a CHANGELOG entry carrying the served
md5s; `ops` carries no app source and no built artifact; `handover` is defined in the tool as a
workbench of unverified work with **no shippable half**. A session that finishes and verifies the app
work but runs out of budget before the CHANGELOG, manifest and TESTING updates is in none of those
states — and that is a normal way for a session to end, not an exotic one. `handover` is the closest
honest declaration and it fails `B-3` and `C-6` by construction, because `handover` shares the `ops`
branch and those two forbid shipping an app source or a built `index.html`. **Filed, not fixed.**
Declaring `ops` to make it pass would be a gate talked round, which is what the `handover` kind was
itself created to stop.

⚠ **Confirmed from the OTHER direction the same day.** The documents-only package that completed
v5.58 declares `app-release` — correctly, it finishes a version — and fails `B-3` and `E-2`, both of
which require a versioned app source in `knowledge/`. It has none, because the app half shipped in
the earlier package and re-shipping it would fail `D-1`. So the gap is not one missing kind but a
missing **axis**: `package_check` assumes a release ships as ONE package, and a release split across
two — app first, documents second — cannot satisfy it in either half. Both halves were verified and
both are legitimately un-passable. **Filed, not fixed**, and worth noting that a split release is the
normal consequence of a session ending on budget, not an unusual event.

⚠ **`H-1`/`H-2`/`J-2` fail pre-upload BY CONSTRUCTION and this is not a defect.** `H` reads the
CHANGELOG that GitHub is actually serving and `J` reads the live pool, so a package containing the
fix necessarily fails them until it is uploaded. They are POST-SHIP checks. **Re-run after uploading**
— that is the whole point of passing the pool as the 4th argument, and a green `H`/`J` before upload
would mean the checks were reading the package rather than reality.

Two defects in `package_check.mjs` and its control harness. They are recorded here rather than in
`OPERATIONS.md` because they are harness behaviour, not a rule. Both had survived two sessions in
chat and in expired `README-FIRST.md` files, which is how an item gets lost.

- **`D-1` fails on every post-ship run.** It is a pre-ship check and the harness carries no mode
  signal, so a package verified *after* it ships reports a failure that means nothing. Passing a
  pool directory is an unambiguous signal that the ship already happened, so `D-1` can gate itself
  off when `POOL` is set. Noted deliberately rather than changed inside the v5.57 release, which was
  already verified.
- **`P17` in `package_check_controls.sh` does not fire**, and did not before v5.57.1 either —
  confirmed by running the identical harness against the unmodified gate. `E-1b` is meant to catch a
  both-destinations file omitted from `github/`; `C-2` and `D-1` catch that case instead, so nothing
  escapes, but the named control is **vacuous**. A control that cannot fail is worse than no control
  (§B2), so this should be repaired rather than retired.

⚠ **The case for repairing `P17` got stronger on 2026-09-01.** A corrected manifest was uploaded as
`PROJECT_KNOWLEDGE_INDEX (1).md` to both destinations, leaving a **stale twin at the repo root under
the canonical name** — the exact condition §G's one-copy rule exists to prevent. **No gate saw it**,
because both files were individually valid and only their *names* were wrong. It was caught by
comparing filenames against §G by hand. Cross-destination naming is the gap.

## What the tests don't cover

No independent professional review of the tax, IRMAA, or ACA modeling has occurred. The app's limitations sections (Field Manual §13, METHODOLOGY §12, and the in-app ACA notes) list the simplifications that remain by design — simplified SS taxability, effective-rate state approximations, deterministic-by-default mortality, and the rest. The suites prove the code implements the documented model; they cannot prove the model matches your life.

The suite also proves nothing about builds it hasn't run against. Counts on this page are pinned to the build named at the top; a different version is a different claim.

## A note on AI-assisted development — and one incident worth disclosing

This project is built with AI assistance, which is exactly why the verification culture above exists: plausible-looking code is not reviewed code. One incident during v5.7 development makes the point concretely. Partway through the build, the AI working copy was found to contain a complete, plausible, *unrequested* ACA implementation — engine, UI, and matching tests — that no one had written or reviewed. Much of it was even substantively correct. It was quarantined rather than adopted: the source was reverted to the last verified snapshot, and the feature was rebuilt deliberately from a written spec, with every constant fetched from primary sources during the build and every test case hand-derived. The same discipline caught two genuine errors in the deliberate rebuild — one in the builder's hand math (the engine was right), one a real bug (a premium-growth term that silently zeroed later bridge years, caught by a test asserting exact compounding).

A second, quieter failure is worth disclosing because this page was its victim: through several releases, this file's headline count drifted badly out of date — claiming 562 checks and a six-suite layout that no longer existed, and stating the suite was unpublished after it had been committed. The per-suite figures in each release's CHANGELOG were correct throughout; this page simply stopped being updated. An earlier release also shipped totals that were understated by a transcription slip in hand addition. Both are the same lesson from different angles, and the reason every count on this page is now computed from parsed suite output rather than restated: the tests are not ceremony; they are the only thing standing between "looks right" and "is right."
